FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY time_mcp_server.py .

EXPOSE 8001

ENV MCP_TIME_PORT=8001

CMD ["python", "time_mcp_server.py"]
